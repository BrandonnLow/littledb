// Command littledb is the CLI entry point.
// For now it just prints a banner so we can verify the build pipeline works.
package main

import "fmt"

func main() {
	fmt.Println("littledb — under construction")
}
